using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// What is a combat stat?
/// A combat stat is influenced by character attributes
/// Each combat stat is effected differently, and it is dependent on the character's league type. Rogue, Warrior, or Wizard.
/// 
/// </summary>
public class CombatStat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
